apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-03.fullname" . }}-test-connection"
  labels:
    {{- include "hello-03.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-03.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
